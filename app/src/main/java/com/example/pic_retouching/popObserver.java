package com.example.pic_retouching;

import com.example.pic_retouching.mainUI.DraftPopupWindow;

// for MainActivity to implement
public interface popObserver {
     void update(int type, int position);
}
